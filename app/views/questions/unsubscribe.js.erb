<% if @notification.errors.present? %>
    alert("Произошла ошибка: #{@notification.errors.full_messages}");
<% else %>
    var $link = $('#subscribtion');
    $link.text('Подписаться');
    $link.attr('href', "#{subscribe_question_path(@notification.question_id}");
<% end %>
